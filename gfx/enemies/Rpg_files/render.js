const render = () => {
    // game.enemy.forEach(enemy => game.debug.body(enemy))
    // game.debug.body(game.player)
    // game.skillShoot && game.debug.body(game.skillShoot)
    // game.anim && game.debug.body(game.anim)
};